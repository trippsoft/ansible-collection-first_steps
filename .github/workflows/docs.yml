---
name: Generate Role Docs
'on':
  workflow_dispatch: {}
  push:
    branches:
      - main
    paths:
      - galaxy.yml
      - roles/debian/.aar_doc.yml
      - roles/debian/README.md
      - roles/debian/meta/argument_specs.yml
      - roles/debian/meta/main.yml
      - roles/fedora/.aar_doc.yml
      - roles/fedora/README.md
      - roles/fedora/meta/argument_specs.yml
      - roles/fedora/meta/main.yml
      - roles/rhel/.aar_doc.yml
      - roles/rhel/README.md
      - roles/rhel/meta/argument_specs.yml
      - roles/rhel/meta/main.yml
      - roles/ubuntu/.aar_doc.yml
      - roles/ubuntu/README.md
      - roles/ubuntu/meta/argument_specs.yml
      - roles/ubuntu/meta/main.yml
defaults:
  run:
    working-directory: 'trippsc2.first_steps'
jobs:
  docs_debian:
    name: Generate debian role documentation
    permissions:
      contents: write
    runs-on:
      - self-hosted
      - linux
      - ansible
      - x64
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          path: trippsc2.first_steps
          ref: main
      - name: Generate debian role documentation
        working-directory: 'trippsc2.first_steps'
        run: |
          source ~/venv/ansible-2.16/bin/activate
          cd ./roles/debian
          aar-doc . markdown
          git config user.name "GitHub Actions"
          git config user.email "github-actions@github.com"
          git add README.md || /bin/true
          git commit -m "Update debian role documentation." || /bin/true
          git push origin main
          deactivate
  docs_fedora:
    name: Generate fedora role documentation
    needs: docs_debian
    permissions:
      contents: write
    runs-on:
      - self-hosted
      - linux
      - ansible
      - x64
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          path: trippsc2.first_steps
          ref: main
      - name: Generate fedora role documentation
        working-directory: 'trippsc2.first_steps'
        run: |
          source ~/venv/ansible-2.16/bin/activate
          cd ./roles/fedora
          aar-doc . markdown
          git config user.name "GitHub Actions"
          git config user.email "github-actions@github.com"
          git add README.md || /bin/true
          git commit -m "Update fedora role documentation." || /bin/true
          git push origin main
          deactivate
  docs_rhel:
    name: Generate rhel role documentation
    needs: docs_fedora
    permissions:
      contents: write
    runs-on:
      - self-hosted
      - linux
      - ansible
      - x64
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          path: trippsc2.first_steps
          ref: main
      - name: Generate rhel role documentation
        working-directory: 'trippsc2.first_steps'
        run: |
          source ~/venv/ansible-2.16/bin/activate
          cd ./roles/rhel
          aar-doc . markdown
          git config user.name "GitHub Actions"
          git config user.email "github-actions@github.com"
          git add README.md || /bin/true
          git commit -m "Update rhel role documentation." || /bin/true
          git push origin main
          deactivate
  docs_ubuntu:
    name: Generate ubuntu role documentation
    needs: docs_rhel
    permissions:
      contents: write
    runs-on:
      - self-hosted
      - linux
      - ansible
      - x64
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          path: trippsc2.first_steps
          ref: main
      - name: Generate ubuntu role documentation
        working-directory: 'trippsc2.first_steps'
        run: |
          source ~/venv/ansible-2.16/bin/activate
          cd ./roles/ubuntu
          aar-doc . markdown
          git config user.name "GitHub Actions"
          git config user.email "github-actions@github.com"
          git add README.md || /bin/true
          git commit -m "Update ubuntu role documentation." || /bin/true
          git push origin main
          deactivate
